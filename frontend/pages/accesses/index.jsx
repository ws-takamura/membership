import Layout from "@/components/Layout/Admin/Layout";

export default function AccessesIndex() {
    return (
        <>
            <p>ACCESS</p>
        </>
    );
}

AccessesIndex.getLayout = (page) => <Layout>{page}</Layout>;
